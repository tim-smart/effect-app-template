{
  "name": "@app/http",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "publishConfig": {
    "access": "restricted",
    "directory": "dist"
  },
  "scripts": {
    "clean": "rimraf build dist",
    "build": "pnpm clean && tsup",
    "build-pkg": "pnpm build && pkg -c package.json dist/index.js",
    "dev": "nodemon",
    "docker": "pnpm build-pkg && docker build",
    "test": "vitest"
  },
  "sideEffects": [],
  "dependencies": {
    "@app/core": "workspace:^0.1.0",
    "@effect/platform-node": "^0.13.8",
    "effect": "2.0.0-next.29"
  },
  "pkg": {
    "targets": [
      "node18-alpine-x64",
      "node18-macos-x64"
    ],
    "outputPath": "build/pkg"
  }
}
